monitor:
  files-delta: 1m
  poll-every: 10s
  directories:
    - "/uploads"
    - "/uploads/sosreport"
  processor-map:
    - type: filename
      regex: ".*sosreport.*.tar.[xz|gz]+$"
      processor: sosreports
